# name: test/sql/close.test
# description: test odbc_close() function
# group: [sql_close]

statement error
SELECT odbc_close()
----
Catalog Error: Scalar Function with name odbc_close does not exist!

require odbc_scanner

statement error
SELECT odbc_close()
----
Binder Error: No function matches the given name and argument types 'odbc_close()'

statement error
SELECT odbc_close(NULL)
----
Invalid Input Error: 'odbc_close' error: specified ODBC connection argument must be not NULL

statement ok
SELECT odbc_close(42)

statement ok
SET VARIABLE conn = odbc_connect('ODBCSCANNER_DEBUG_CONN_STRING_ENV_VAR=ODBC_CONN_STRING')

statement ok
SELECT odbc_close(getvariable('conn'))
